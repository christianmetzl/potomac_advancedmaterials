# CPU-verified core (Python 3.11) — installs cleanly on a CPU-only box.
# This is all you need to reproduce every CPU headline result (see README "Reproduce").
pyscf
openfermion
openfermionpyscf
h5py
pennylane
pennylane-lightning
torch
scipy
numpy
matplotlib

# GPU / tensor-network extras for the Phase-3 qBraid runs are in requirements-gpu.txt
# (cudaq, quimb, block2) — do NOT install those on a CPU-only machine; they will fail.
