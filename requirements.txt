# CPU-verified core (Python 3.11) — installs cleanly on a CPU-only box.
# This is all you need to reproduce the core CPU headline results (see README "Reproduce").
pyscf
openfermion
openfermionpyscf
h5py
pennylane
pennylane-lightning
torch
scipy
numpy
matplotlib

# Optional CPU extras — also install cleanly on a CPU-only box and reproduce two more results:
#   cudaq  -> qpp-cpu backend executes the GQE/QSCI circuits (src/cudaq_qsci.py)
#   block2 -> DMRG / MPS bond-dimension + entanglement study (src/mps_bonddim_study.py, src/dmrg_scale.py)
# These run WITHOUT a GPU. reproduce.py auto-detects them and runs the extra checks if present.
cudaq
block2
qbraid
qiskit
pennylane-qiskit

# GPU is required only for the AT-SCALE Phase-3 runs (40q tensornet-mps / cuStateVec); see
# requirements-gpu.txt. The packages themselves (cudaq, block2) install and run on CPU.
